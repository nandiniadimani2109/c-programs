#include <stdio.h>
int main() 
{
    int age;
    printf("Enter your age:\n ");
    scanf("%d", &age);
    if (age > 40)
    {
        printf("the person is aged.\n");
    } 
    else 
    {
        printf("the person is not aged.\n");
    }

    return 0;
}
